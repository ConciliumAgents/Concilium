# Concilium Runtime

This directory contains the Concilium runtime.

The directory name `loop-engine` is historical. It is kept during the tool preview so existing launcher scripts, tests, and local workflows remain stable.
